name: Java CI

on: [push]

jobs:
  ping:
    runs-on: self-hosted
    
    steps:
      - uses: actions/checkout@v2
      - name: ping  
        run: chmod u+x ping.sh && ./ping.sh > output.txt && cat output.txt 
        shell: bash
